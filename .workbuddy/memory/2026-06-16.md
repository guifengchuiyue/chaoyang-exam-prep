## 2026-06-16

### 创建"朝阳社工备考冲刺小助手"项目
- 完成完整项目搭建：app.py（主应用）、config.py（配置）、database.py（数据库层）、utils.py（工具函数）
- 实现三大核心模块：
  1. 区情题库：粘贴政策文本 → AI 生成单选题(5)+多选题(3)+判断题(2)，含提交答案、查看解析、加入错题本
  2. 错题本：记录错题、按题型筛选、错题重做模式（答对自动移除）、掌握率统计
  3. 面试模拟：5个北京社区治理场景，四维度 AI 点评（专业价值观/沟通技巧/合规性/创新思维），提供高分示范答案
- 数据库：SQLite，4张表（wrong_questions, study_progress, interview_history, app_settings）
- 依赖：streamlit 1.58.0, requests 2.34.2
- 所有文件语法验证通过，依赖安装成功
- 修复 config.py：`st.secrets` 在无 secrets.toml 时直接报错，改为 try/except 安全获取
- 通过环境变量注入 DeepSeek API Key，应用正常启动在 localhost:8501
- 创建 `.streamlit/secrets.toml`，填入 DeepSeek API 配置，应用无需环境变量也能读取密钥
- 修复 app.py 第151行：`st.image(...) if False else st.markdown(...)` 的行内三元表达式触发 Streamlit AST 解析错误，改为直接 `st.markdown("## 📚")`
- **重大重构**：彻底重写项目为"全真模拟与专项刷题系统"
  - 删除旧文件（app.py, config.py, database.py, utils.py），新建 app.py + database.py + llm_service.py
  - 核心变化：无需用户上传资料，内置 5 大板块专家 Prompt，点击即自动生成题目
  - 5 大板块：公共管理、法律基础、社会建设与社会工作、朝阳区区情、基本能力
  - 新增全真模拟卷（20题，倒计时，雷达图评分，逐题解析）
  - 新增学习报告模块（趋势图、饼图、每日统计）
  - 依赖新增：pandas, plotly
  - 应用在 localhost:8501 正常运行
- 修复 SQLite 错误：旧数据库无 knowledge_point 列，删除 data/exam_system.db 重建
- 修复 UI 显示问题：选项不再重复显示（题目区域只显示题目文本，选项统一在控件中展示）
- 优化交互提示：单选显示"请选择一个选项："，多选显示"请勾选所有正确的选项（可多选）："，判断显示"请判断对错："
